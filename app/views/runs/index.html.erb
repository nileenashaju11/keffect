<nav class="level">
  <!-- Left side -->
  <div class="level-left">
    <div class="level-item"><h1 class="title">Runs</h1></div>
  </div>
  <!-- Right side -->
  <div class="level-right">
    <div class="level-item"><%= link_to '+ New Run', new_run_path, class: 'button is-link is-rounded has-text-weight-semibold' %></div>
  </div>
</nav>

<%= render partial: 'table', locals: { runs: @runs } %>
